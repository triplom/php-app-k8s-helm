# Secret values for php-app Helm chart.
# Copy this file to helm-values-secret.yaml, fill in real credentials,
# and NEVER commit helm-values-secret.yaml to version control.

mariadb:
  auth:
    rootPassword: "CHANGE_ME_ROOT_PASSWORD"
    password: "CHANGE_ME_APP_PASSWORD"
    username: appuser
    database: appdb
